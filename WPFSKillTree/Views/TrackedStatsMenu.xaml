<dialogs:CloseableBaseDialog
    x:Class="POESKillTree.Views.TrackedStatsMenu"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:l="clr-namespace:POESKillTree.Localization.XAML" xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d"
    xmlns:dialogs="clr-namespace:POESKillTree.Controls.Dialogs"
    xmlns:converter="clr-namespace:POESKillTree.Utils.Converter"
    MaxContentWidth="550">
    <ContentControl.Resources>
        <Style x:Key="TabControlStyle1" TargetType="{x:Type TabControl}">
            <Setter Property="Background" Value="{x:Null}"/>
            <Setter Property="BorderBrush" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type TabControl}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition x:Name="ColumnDefinition0"/>
                                <ColumnDefinition x:Name="ColumnDefinition1" Width="0"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition x:Name="RowDefinition0" Height="Auto"/>
                                <RowDefinition x:Name="RowDefinition1" Height="*"/>
                            </Grid.RowDefinitions>
                            <UniformGrid x:Name="HeaderPanel" Grid.Column="0" Rows="1" IsItemsHost="True" Grid.Row="0" KeyboardNavigation.TabIndex="1" Panel.ZIndex="1"/>
                            <Border x:Name="ContentPanel" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}" Grid.Column="0" KeyboardNavigation.DirectionalNavigation="Contained" Grid.Row="1" KeyboardNavigation.TabIndex="2" KeyboardNavigation.TabNavigation="Local">
                                <ContentPresenter x:Name="PART_SelectedContentHost" ContentTemplate="{TemplateBinding SelectedContentTemplate}" Content="{TemplateBinding SelectedContent}" ContentStringFormat="{TemplateBinding SelectedContentStringFormat}" ContentSource="SelectedContent" Margin="{TemplateBinding Padding}" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                            </Border>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="TabStripPlacement" Value="Bottom">
                                <Setter Property="Grid.Row" TargetName="HeaderPanel" Value="1"/>
                                <Setter Property="Grid.Row" TargetName="ContentPanel" Value="0"/>
                                <Setter Property="Height" TargetName="RowDefinition0" Value="*"/>
                                <Setter Property="Height" TargetName="RowDefinition1" Value="Auto"/>
                                <Setter Property="Margin" TargetName="HeaderPanel" Value="2,0,2,2"/>
                            </Trigger>
                            <Trigger Property="TabStripPlacement" Value="Left">
                                <Setter Property="Grid.Row" TargetName="HeaderPanel" Value="0"/>
                                <Setter Property="Grid.Row" TargetName="ContentPanel" Value="0"/>
                                <Setter Property="Grid.Column" TargetName="HeaderPanel" Value="0"/>
                                <Setter Property="Grid.Column" TargetName="ContentPanel" Value="1"/>
                                <Setter Property="Width" TargetName="ColumnDefinition0" Value="Auto"/>
                                <Setter Property="Width" TargetName="ColumnDefinition1" Value="*"/>
                                <Setter Property="Height" TargetName="RowDefinition0" Value="*"/>
                                <Setter Property="Height" TargetName="RowDefinition1" Value="0"/>
                                <Setter Property="Margin" TargetName="HeaderPanel" Value="2,2,0,2"/>
                            </Trigger>
                            <Trigger Property="TabStripPlacement" Value="Right">
                                <Setter Property="Grid.Row" TargetName="HeaderPanel" Value="0"/>
                                <Setter Property="Grid.Row" TargetName="ContentPanel" Value="0"/>
                                <Setter Property="Grid.Column" TargetName="HeaderPanel" Value="1"/>
                                <Setter Property="Grid.Column" TargetName="ContentPanel" Value="0"/>
                                <Setter Property="Width" TargetName="ColumnDefinition0" Value="*"/>
                                <Setter Property="Width" TargetName="ColumnDefinition1" Value="Auto"/>
                                <Setter Property="Height" TargetName="RowDefinition0" Value="*"/>
                                <Setter Property="Height" TargetName="RowDefinition1" Value="0"/>
                                <Setter Property="Margin" TargetName="HeaderPanel" Value="0,2,2,2"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <converter:EnumToDescriptionConverter x:Key="EnumToDescriptionConverter" />
        <converter:EnumToBoolConverter x:Key="EnumToBoolConverter" />
        <converter:InvertBoolConverter x:Key="InvertBoolConverter" />
        <converter:FlagsToBoolConverter x:Key="FlagsToBoolConverter" />
        <ObjectDataProvider ObjectInstance="{x:Type Brushes}"
                            MethodName="GetProperties"
                            x:Key="Brushes" />
        <Style x:Key="BrushComboBox" TargetType="{x:Type ComboBox}" BasedOn="{StaticResource {x:Type ComboBox}}">
            <Setter Property="ItemsSource" Value="{Binding Source={StaticResource Brushes}}" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="SelectedValuePath" Value="Name" />
            <Setter Property="DisplayMemberPath" Value="Name" />
            <Setter Property="Foreground" Value="{Binding SelectedValue, RelativeSource={RelativeSource Self}}" />
            <Setter Property="ItemContainerStyle">
                <Setter.Value>
                    <Style TargetType="ComboBoxItem" BasedOn="{StaticResource {x:Type ComboBoxItem}}">
                        <Setter Property="Foreground" Value="{Binding Name}" />
                    </Style>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="SectionLabel" TargetType="{x:Type Label}" BasedOn="{StaticResource {x:Type Label}}">
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Height" Value="30" />
            <Setter Property="FontSize" Value="{DynamicResource DialogMessageFontSize}"></Setter>
        </Style>
        <Style x:Key="SettingLabel" TargetType="{x:Type Label}" BasedOn="{StaticResource {x:Type Label}}">
            <Setter Property="VerticalContentAlignment" Value="Center" />
        </Style>
    </ContentControl.Resources>
    <StackPanel Orientation="Vertical">
        <Grid Margin="20 0 5 0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="250"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="30" />
                <RowDefinition Height="30" />
                <RowDefinition Height="30" />
                <RowDefinition Height="30" />
                <RowDefinition Height="30" />
            </Grid.RowDefinitions>

            <Label Style="{StaticResource SettingLabel}"
                   Grid.Row="0" Grid.Column="0">
                <Label.Content>
                    <l:Catalog Message="TrackedStats Path" />
                </Label.Content>
            </Label>
            <Grid Grid.Row="0" Grid.Column="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition MinWidth="50" />
                </Grid.ColumnDefinitions>
                <TextBlock Text="{Binding TrackingOptions.StatTrackingSavePath}"
                           VerticalAlignment="Center"
                           Margin="0 0 5 0"
                           ToolTip="{Binding TrackingOptions.StatTrackingSavePath}"/>
                <Button Grid.Column="1"
                        Command="{Binding ChangeStatTrackingPathCommand}">
                    <Button.Content>
                        <l:Catalog Message="Change..." />
                    </Button.Content>
                </Button>
            </Grid>

            <Button Grid.Row="1" Grid.Column="1"
                        Command="{Binding LoadTrackedStatFileNames}" Grid.ColumnSpan="2" Margin="0,0,95,0">
                <Button.Content>
                    <l:Catalog Message="Load Tracked Stat File Names" />
                </Button.Content>
            </Button>

            <ComboBox Grid.Row="2" DataContext="{Binding TrackingOptions.TrackingFileList,NotifyOnSourceUpdated=True, UpdateSourceTrigger=PropertyChanged}"
            VerticalAlignment="Center" Margin="0,4,95,4"
                           ToolTip="Select Target TrackedStats file" Grid.ColumnSpan="2"/>

            <Button Grid.Row="3"
                        Command="{Binding LoadTrackedStats}" Grid.ColumnSpan="2" Margin="0,0,95,0">
                <Button.Content>
                    <l:Catalog Message="Load Tracked Stats from file" />
                </Button.Content>
            </Button>
            <Button Grid.Row="4" Grid.ColumnSpan="2" Margin="0,0,95,0" Click="ResetTracking">
                <Button.Content>
                    <l:Catalog Message="Reset Tracking" />
                </Button.Content>
            </Button>
        </Grid>
    </StackPanel>
</dialogs:CloseableBaseDialog>